meta {
  name: Gateway - Get Image By Id (no ext)
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/api/v1/images/28cccf5cc0a4
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Content-Type is an image", function() {
    const contentType = res.getHeader('content-type');
    expect(contentType).to.match(/^image\//);
  });
}
