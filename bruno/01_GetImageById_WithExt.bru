meta {
  name: Gateway - Get Image By Id (with ext)
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/api/v1/images/28cccf5cc0a4?ext=png
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Content-Type is image/png", function() {
    const contentType = res.getHeader('content-type');
    expect(contentType).to.contain('image/png');
  });
}
