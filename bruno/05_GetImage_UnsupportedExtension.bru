meta {
  name: Gateway - Get Image (unsupported extension)
  type: http
  seq: 5
}

get {
  url: http://localhost:3000/api/v1/images/28cccf5cc0a4?ext=exe
  body: none
  auth: none
}

tests {
  test("Status is 400", function() {
    expect(res.getStatus()).to.equal(400);
  });

  test("Error message is returned", function() {
    const body = res.getBody();
    expect(body.error).to.equal('Unsupported extension');
    expect(body.allowed).to.include('png');
  });
}
